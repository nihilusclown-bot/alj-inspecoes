# Paste this into Streamlit Cloud → Settings → Secrets

APP_URL = "https://alj447.streamlit.app"
# Each line must be on its own line (do not put everything on one line)

[connections.supabase]
dialect = "postgresql"
host = "aws-1-us-east-1.pooler.supabase.com"
port = 5432
database = "postgres"
username = "postgres.viqwzdcrmutgfvebszrg"
password = "your-supabase-database-password"